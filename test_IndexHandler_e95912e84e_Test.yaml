# Test generated by RoostGPT for test News-App-test using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          expect:
            - statusCode: 200
            - hasProperty: "data"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/nonexistent-path"
          expect:
            - statusCode: 404
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/?param=verylongstringverylongstringverylongstringverylongstringverylongstringverylongstring"
          expect:
            - statusCode: 200
            - hasProperty: "data"
